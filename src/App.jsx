import {createTheme, ThemeProvider} from "@mui/material";

function App() {

    return (
        <ThemeProvider theme={theme}>
           App
        </ThemeProvider>
    )
}

const theme = createTheme({
   
})

export default App
